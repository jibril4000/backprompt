---
description: Show a stretch card right now — no waiting for the timer
allowed-tools: Bash
---

!`"${CLAUDE_PLUGIN_ROOT}/scripts/check-stretch.sh" now`

The user asked backprompt for an immediate stretch; the card above was
generated for them and the plugin's timer has already been updated. Present
the card exactly as shown, inside a fenced code block with spacing
preserved, no commentary before it and at most one short encouraging line
after it. Do not invent a different stretch.
